// Create a es6 module with function getName, getSurname and default export getFullname.
import getFullName, { getName, getSurname } from "./es6.js"; // Note the .js in the end
console.log(getName("Kiran"), getSurname("Dash"), getFullName("Kiran", "Dash"));

// Create the same with commonJS module


// What is the difference?
